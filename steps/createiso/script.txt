cd {automationWorkerBaseDir}/build-{newOsNode.fqn}

# mkisofs -o {automationWorkerBaseDir}/autoinstaller-{newOsNode.fqn}.iso \
#   -b isolinux/isolinux.bin -c isolinux/boot.cat \
#   -no-emul-boot -boot-load-size 4 -boot-info-table -J -l -V "AI_UBUNTU" .

xorriso -as mkisofs \
  -r -V "AI_UBUNTU" \
  -o {automationWorkerBaseDir}/autoinstaller-{newOsNode.fqn}.iso \
  -J -joliet-long \
  -b boot/grub/i386-pc/eltorito.img \
  -c boot.catalog \
  -no-emul-boot \
  -boot-load-size 4 \
  -boot-info-table \
  .